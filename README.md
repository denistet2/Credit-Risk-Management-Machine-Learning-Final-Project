# Разработка модели кредитного скоринга (Credit Risk Scoring)

> ℹ️ **Примечание:** Проект выполнен в рамках финальной работы курса **Machine Learning Junior (Skillbox)**.
> В работе использовались алгоритмы ИИ в качестве ассистента для оптимизации кода (обработка Big Data), генерации структуры и оформления документации.

## 📋 Описание проекта
**Цель:** Разработать ML-модель для автоматизации принятия решений по выдаче кредитов. Модель должна предсказывать вероятность дефолта (невозврата) клиента на основе его кредитной истории.

**Главный вызов:** Обработка массива данных объемом **>4.5 Гб** (история транзакций и заявок) в условиях ограниченной оперативной памяти (Local PC).

**Стек:** Python, Pandas, Sklearn (Pipeline), CatBoost/HistGradientBoosting, Matplotlib, Tqdm (для чанков).

## 💼 Бизнес-задачи
Необходимо было решить технические и бизнесовые проблемы:
1. Как обработать 12 тяжелых файлов (Parquet) и собрать их в один датасет, не «уронив» память?
2. Какие признаки из кредитной истории реально влияют на вероятность дефолта?
3. Построить устойчивую модель, которая не переобучится на шуме.
4. Достичь метрики качества, достаточной для внедрения MVP.

## 🛠 Этапы работы
1. **Предобработка Big Data (ETL):**
   - Реализована **потоковая обработка (Chunking)**: чтение данных кусками, агрегация «на лету» и удаление мусора из памяти.
   - Сжатие данных: 4.5 Гб сырой истории трансформированы в таблицу из 3 млн профилей клиентов.
   - Feature Engineering: Созданы агрегированные признаки (средние суммы, макс. просрочки, частота кредитов).

2. **Моделирование:**
   - Построен Sklearn Pipeline: `Imputer` (заполнение пропусков) -> `Scaler` (масштабирование) -> `Model`.
   - Использован градиентный бустинг (**HistGradientBoostingClassifier**) как наиболее эффективный для больших табличных данных.
   - Валидация: отложенная выборка (Train/Test split).

3. **Визуализация результатов:**
   - Построена ROC-кривая для оценки качества ранжирования.
   ![ROC Curve](images/ROC-Curve.png)
   *(Примечание: График показывает уверенное превосходство модели над случайным выбором)*

## 📊 Ключевые выводы (Инсайты)
По результатам моделирования выявлены следующие закономерности:

1. **Метрика качества:**
   - Достигнут **ROC-AUC = 0.72**.
   - Это уровень **Stable MVP** (Минимально жизнеспособный продукт). Разрыв между метриками на Train и Test минимален (<2%), что говорит об отличной обобщающей способности модели.

2. **Ограничения данных:**
   - Исходные признаки (суммы, сроки) были **бинаризованы** (закодированы) поставщиком данных.
   - *Инсайт:* Из-за этого невозможно рассчитать точные финансовые коэффициенты (PTI, DTI), которые обычно дают буст метрики до 0.75+. Текущая модель выжала максимум из доступной "физики" данных.

3. **Техническое решение:**
   - Доказано, что задачу Big Data можно решать на обычном ноутбуке без использования кластеров (Spark), если грамотно применять агрегацию и оптимизацию типов данных (`float32` вместо `float64`).

## 📂 Структура репозитория
- `01_EDA_and_Data_Review.ipynb`: Разведка данных и анализ структуры файлов.
- `02_Feature_Engineering.ipynb`: **[Core]** Скрипт потоковой обработки и генерации датасета.
- `03_Training.ipynb`: Обучение пайплайна и валидация.
- `04_Visualization.ipynb`: Графики метрик и анализ ошибок.
- `05_Inference.ipynb`: Генерация прогноза для 3 млн клиентов (`submission_final.csv`).
- `requirements.txt`: Список библиотек для воспроизведения среды.

---
*Проект выполнен Денисом Тетеруковым.*